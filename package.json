{
  "name": "rail-track-app",
  "version": "1.0.0",
  "description": "Rail Track Restoration System - Full Stack",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "cd frontend && npm install && npm run build",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "netlify-cli": "^23.9.1"
  }
}
